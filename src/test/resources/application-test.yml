spring:
  data:
    mongodb:
      auto-index-creation: true
      database: testdb

logging:
  level:
    root: INFO
    com.algolia.fetcher: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

server:
  jwt:
    key: test-secret-key-1234567890-1234567890-1234567890
    algorithm: HS256
    issuer: test-issuer
    expiresIn: 1h
